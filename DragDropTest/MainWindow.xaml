<Window x:Class="DragDropTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DragDropTest"
        mc:Ignorable="d" Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800" Background="White">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="MyGenTextBox" MinWidth="200"/>
            <TextBlock x:Name="MyGenTextBox2" MinWidth="200"/>
        </StackPanel>
        <Canvas Background="White" Grid.Row="1" x:Name="MyBackCanvas" AllowDrop="True" PreviewMouseMove="MyCanvas_PreviewMouseMove" MouseUp="MyCanvas_MouseUp" MouseDown="MyCanvas_MouseDown"/>
        <Canvas Background="Transparent" Grid.Row="1" x:Name="MyCanvas" AllowDrop="True"  PreviewMouseMove="MyCanvas_PreviewMouseMove" MouseUp="MyCanvas_MouseUp" MouseDown="MyCanvas_MouseDown"/>
    </Grid>
</Window>
