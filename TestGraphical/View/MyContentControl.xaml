<UserControl x:Class="TestGraphical.View.MyContentControl"
             x:Name="MyContentControlWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestGraphical.View"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" Loaded="UserControl_Loaded"
             xmlns:main="clr-namespace:TestGraphical"
             xmlns:view="clr-namespace:TestGraphical.View"
             xmlns:ctrl="clr-namespace:TestGraphical.Controls"
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding VM_Workflow}">
        
    <Grid Background="Pink">
        <TextBlock Text="TO JEST TEST WYŚWIETLANIA TYLKO" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Aquamarine"/>

        <Grid x:Name="MyContentGrid">

            <Canvas x:Name="BackCanvas" 
                Background="Transparent" MouseDown="MyCanvas_MouseDown" 
                MouseUp="BackCanvas_MouseUp" MouseMove="BackCanvas_MouseMove"
                PreviewMouseMove="BackCanvas_PreviewMouseMove" >
            </Canvas>
            <Canvas x:Name="MyCanvas"  >

            </Canvas>
        </Grid>

  
    </Grid>

</UserControl>
